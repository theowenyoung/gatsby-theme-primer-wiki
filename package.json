{
  "private": true,
  "scripts": {
    "start": "npm -w wiki run develop",
    "clean": "npm -w wiki run clean",
    "release": "changeset publish",
    "build": "npm -w wiki run build",
    "serve": "npm -w wiki run serve",
    "test": "npm -ws run test -- --passWithNoTests"
  },
  "workspaces": [
    "theme",
    "wiki",
    "gatsby-transformer-summary",
    "gatsby-remark-rewrite-link-for-trailing-slash"
  ],
  "dependencies": {
    "@changesets/changelog-github": "^0.4.0",
    "@changesets/cli": "^2.16.0"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
