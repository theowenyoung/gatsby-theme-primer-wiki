{
  "private": true,
  "scripts": {
    "start": "npm -w example run develop",
    "clean": "npm -w example run clean",
    "release": "changeset publish",
    "build": "npm -w example run build",
    "serve": "npm -w example run serve",
    "test": "npm -ws run test -- --passWithNoTests"
  },
  "workspaces": [
    "references",
    "remove-brackets",
    "rewrite",
    "summary",
    "theme",
    "example",
    "slug",
    "transformer-url"
  ],
  "dependencies": {
    "@changesets/changelog-github": "^0.4.0",
    "@changesets/cli": "^2.16.0"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
